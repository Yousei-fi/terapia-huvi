{% extends "base.twig" %}

{% block content %}
<section class="py-16 md:py-20">
  <div class="mx-auto max-w-3xl px-4">
    <h1 class="section-heading text-white">{{ post.title }}</h1>
    <div class="prose prose-invert prose-lg max-w-none mt-8 text-white/80">
      {{ post.content }}
    </div>
  </div>
</section>
{% endblock %}

