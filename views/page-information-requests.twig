{% extends "base.twig" %}

{% block content %}
  {% set inferred_language = language ?? 'fi' %}
  {% include 'partials/requests-page.twig' with {
    lang: inferred_language == 'fi' ? 'en' : inferred_language,
    slug: 'requests-page'
  } %}
{% endblock %}

