{% extends "base.twig" %}

{% block content %}
  {% set inferred_language = language ?? 'fi' %}
  {% include 'partials/involvement-page.twig' with {
    lang: inferred_language == 'fi' ? 'en' : inferred_language,
    slug: 'osallistu'
  } %}
{% endblock %}

